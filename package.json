{
  "name": "secnews-bot",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node bot.js",
    "pipeline": "node run.js",
    "pipeline:dry": "node run.js --dry-run",
    "pipeline:ollama": "node run.js --provider=ollama",
    "test": "vitest run"
  },
  "dependencies": {
    "@slack/bolt": "^4.1.0",
    "dotenv": "^17.3.1",
    "fast-xml-parser": "^5.2.0"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
